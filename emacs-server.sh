echo ${TMPDIR}

serverdir="/tmp/emacs${UID}/doom"

echo ${serverdir}
echo ${UID}
echo ${SOCKET}

echo ${s="/tmp/emacs${UID}/doom"}
# for file in ${serverdir}/*; do
#     if [[ -S ${file} ]]; then
#         servers += ("${file##*/}")
#     fi
# done

# echo "${servers[@]}"
